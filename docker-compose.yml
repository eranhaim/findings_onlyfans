services:
  server:
    build: ./server
    restart: unless-stopped
    environment:
      PORT: "5000"
      MONGODB_URI: ${MONGODB_URI:-mongodb+srv://eranhaim321_db_user:PtILGzcZXHFeFI0X@cluster0.arkaqdy.mongodb.net/}
      ADMIN_PASSWORD: ${ADMIN_PASSWORD:-Eran123}

  client:
    build:
      context: ./client
      args:
        VITE_API_URL: /api
    restart: unless-stopped
    ports:
      - "${APP_PORT:-3000}:80"
    depends_on:
      - server
