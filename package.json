{
  "name": "findings-website",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "dev:client": "cd client && npm run dev",
    "dev:server": "cd server && node index.js",
    "seed": "cd server && node seed.js",
    "install:all": "cd client && npm install && cd ../server && npm install"
  }
}
